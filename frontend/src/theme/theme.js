const colors = {
  bg: '#0b0f1a',
  card: '#151a2d',
  surface: '#1e2440',
  primary: '#6270ff',
  accent: '#9fb5ff',
  text: '#ffffff'
};

const navigationTheme = {
  dark: true,
  colors: {
    primary: colors.primary,
    background: colors.bg,
    card: colors.card,
    text: colors.text,
    border: '#222a45',
    notification: colors.accent
  }
};

export default { colors, navigationTheme };
